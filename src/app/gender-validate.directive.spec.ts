import { GenderValidateDirective } from './gender-validate.directive';

describe('GenderValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderValidateDirective();
    expect(directive).toBeTruthy();
  });
});
